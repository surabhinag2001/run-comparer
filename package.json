{
  "name": "run-comparer-app",
  "version": "1.0.0",
  "description": "Compare two or more Strava runs. Anyone can connect their own Strava account, pick runs, and share a comparison link with someone else without either of you sharing credentials.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5",
    "cookie-parser": "^1.4.6"
  }
}
